# Development & Testing Requirements
# NOTE: Does NOT include -r requirements.txt to avoid version conflicts on CI.
# Pins are intentionally loose so GitHub Actions can resolve them on Python 3.10.

# Core data (CI-compatible loose pins)
pandas>=1.5,<3.0
numpy>=1.23,<2.5
scipy>=1.9,<2.0
scikit-learn>=1.1,<2.0
joblib>=1.2

# ML models
xgboost>=1.7,<3.0
lightgbm>=3.3,<5.0
scikit-surprise>=1.1

# API layer — all packages imported by api/main.py
fastapi>=0.100,<1.0
uvicorn>=0.20
pydantic>=2.0,<3.0
python-multipart>=0.0.6
slowapi>=0.1.9
httpx>=0.24,<1.0
requests>=2.28
python-dotenv>=1.0

# Monitoring — imported at the top of api/main.py
prometheus-fastapi-instrumentator>=6.0
prometheus-client>=0.14

# MLflow (skinny = no heavy deps)
mlflow-skinny>=2.0

# Testing
pytest>=7.4
pytest-asyncio>=0.21
pytest-cov>=4.0

# Lint tools (pinned so lint job and test job stay in sync)
flake8>=7.0
black>=24.0
isort>=5.13
